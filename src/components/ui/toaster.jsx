
import React from 'react';
import { toast } from 'sonner';

// Simple wrapper component for Sonner toast
export function Toaster() {
  return null; // Sonner provides its own UI via the <Toaster /> component
}

export { toast };
